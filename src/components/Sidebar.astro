---
// 可傳入的參數
interface Props {
    // 這裡可定義需要的參數
}
---

<div class="sidebar p-6 flex flex-col h-full">
    <!-- 公司資訊或使用者資訊區塊 -->
    <div class="flex flex-col gap-4 items-center text-center sm:items-start sm:text-left mb-6">
        <div class="flex flex-col sm:flex-row gap-4 sm:items-center">
            <!-- Logo 或頭像 -->
            <div class="w-24 h-24 rounded-xl bg-gray-700 border-2 border-primary mx-auto sm:mx-0">
                <!-- Logo 或頭像的 placeholder -->
            </div>

            <div>
                <h1 class="text-2xl font-extrabold mb-1 inline-block relative text-text-primary">
                    公司名稱
                </h1>

                <p class="text-sm mb-2 text-primary">
                    專業服務提供者
                </p>

                <p class="flex items-center justify-center sm:justify-start text-sm text-gray-400">
                    <svg class="-mt-0.5 inline mr-2" fill="none" height="14" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" viewBox="0 0 24 24" width="14"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"></path><circle cx="12" cy="10" r="3"></circle></svg>
                    台北市中山區
                </p>
            </div>
        </div>

        <p class="text-sm leading-relaxed mt-4 text-gray-400 text-justify">
            專業的服務團隊，提供高品質的解決方案，滿足客戶各種需求。我們致力於創新與卓越，為您帶來最佳體驗。
        </p>
    </div>
    
    <!-- 互動式終端機樣式區塊 -->
    <div class="mt-6 bg-black rounded-lg p-4 border border-gray-600 text-xs">
        <div class="flex items-center mb-2">
            <span class="text-primary mr-2">$</span>
            <span class="text-gray-100 typing-command"></span>
        </div>

        <div class="mt-2 text-gray-400">
            <div class="terminal-line init-text hidden"></div>
            <div class="terminal-line ready-text hidden"></div>
            <div class="terminal-line waiting-text hidden"></div>
        </div>
    </div>
    
    <!-- 社交媒體連結 -->
    <div class="flex flex-wrap gap-3 mt-6 justify-between mt-auto">
        <a href="#" target="_blank" class="flex items-center justify-center w-10 h-10 rounded-lg bg-black text-gray-100 transition-all duration-150 border border-gray-600 hover:bg-primary hover:-translate-y-0.5">
            <svg fill="none" height="24" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" viewBox="0 0 24 24" width="24"><path d="M18 2h-3a5 5 0 0 0-5 5v3H7v4h3v8h4v-8h3l1-4h-4V7a1 1 0 0 1 1-1h3z"></path></svg>
        </a>
        
        <a href="#" target="_blank" class="flex items-center justify-center w-10 h-10 rounded-lg bg-black text-gray-100 transition-all duration-150 border border-gray-600 hover:bg-primary hover:-translate-y-0.5">
            <svg fill="none" height="24" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" viewBox="0 0 24 24" width="24"><path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"></path><rect height="12" width="4" x="2" y="9"></rect><circle cx="4" cy="4" r="2"></circle></svg>
        </a>
        
        <a href="#" target="_blank" class="flex items-center justify-center w-10 h-10 rounded-lg bg-black text-gray-100 transition-all duration-150 border border-gray-600 hover:bg-primary hover:-translate-y-0.5">
            <svg fill="none" height="24" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" viewBox="0 0 24 24" width="24"><path d="M23 3a10.9 10.9 0 0 1-3.14 1.53 4.48 4.48 0 0 0-7.86 3v1A10.66 10.66 0 0 1 3 4s-4 9 5 13a11.64 11.64 0 0 1-7 2c9 5 20 0 20-11.5a4.5 4.5 0 0 0-.08-.83A7.72 7.72 0 0 0 23 3z"></path></svg>
        </a>
        
        <a href="mailto:info@company.com" class="flex items-center justify-center w-10 h-10 rounded-lg bg-black text-gray-100 transition-all duration-150 border border-gray-600 hover:bg-primary hover:-translate-y-0.5">
            <svg fill="none" height="24" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" viewBox="0 0 24 24" width="24"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"></path><polyline points="22,6 12,13 2,6"></polyline></svg>
        </a>
    </div>
</div>

<script>
    // 打字效果
    document.addEventListener('DOMContentLoaded', () => {
        const commandElement = document.querySelector('.typing-command');
        const initTextElement = document.querySelector('.init-text');
        const readyTextElement = document.querySelector('.ready-text');
        const waitingTextElement = document.querySelector('.waiting-text');
        
        if (commandElement && initTextElement && readyTextElement && waitingTextElement) {
            const typeText = (element, text, speed, callback) => {
                let i = 0;
                element.textContent = '';
                element.classList.remove('hidden');
                
                const typing = setInterval(() => {
                    if (i < text.length) {
                        element.textContent += text.charAt(i);
                        i++;
                    } else {
                        clearInterval(typing);
                        if (callback) callback();
                    }
                }, speed);
            };
            
            const showText = (element, text, callback) => {
                element.textContent = text;
                element.classList.remove('hidden');
                if (callback) callback();
            };
            
            // 執行動畫序列
            typeText(commandElement, 'npx connect', 180, () => {
                setTimeout(() => {
                    showText(initTextElement, '初始化中...', () => {
                        setTimeout(() => {
                            showText(readyTextElement, '準備就緒。', () => {
                                setTimeout(() => {
                                    showText(waitingTextElement, '等待連線請求...');
                                }, 2000);
                            });
                        }, 1400);
                    });
                }, 1500);
            });
        }
    });
</script>